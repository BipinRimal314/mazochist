node_modules
dist
.DS_Store
__pycache__/
*.pyc
.env.local
.env
.vercel

# Supabase CLI scratch: which project this machine is linked to, cached
# version strings. Machine-local, and re-created by `supabase link`.
supabase/.temp
src-tauri/target
src-tauri/gen

# board renders from `npm run shots` — regenerate rather than commit
shots/

# Vercel CLI appends this; .env.example is the documented template and is
# meant to be committed, so it is excepted back in.
.env*
!.env.example
